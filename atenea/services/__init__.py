"""
atenea/services/ — Pure business logic layer (UI-agnostic)

All functions in this package return data structures (dicts, lists).
None import Rich, NiceGUI, Click, or any display library.

Both CLI (cli.py + tui.py) and Web (web/) frontends call these services.
"""
